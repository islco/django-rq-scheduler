machine:
  python:
    version: 3.6.6
  services:
    - postgresql
    - redis
  environment:
    DB_NAME: circle_ci
    DB_USER: ubuntu
dependencies:
  override:
    - pip install -U pip setuptools
    - pip install -e .[test]
test:
  override:
    - ./testproject19/manage.py test
